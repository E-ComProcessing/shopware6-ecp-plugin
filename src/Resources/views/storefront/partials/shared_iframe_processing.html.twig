{% block base_footer %}
    {% if isIframeEnabled() %}
        {% set currentRoute = app.request.attributes.get('_route') %}
        <script>
            const paymentMethodId = '{{ paymentMethodId() }}';
            const cartValidationUrl = '{{ path('ecomprocessing.frontend.cart.validate') }}';
            const currentRoute = '{{ currentRoute }}';
        </script>
        <script src="{{ asset('bundles/ecomprocessinggenesis/redirect-interceptor.js') }}"></script>
    {% endif %}
{% endblock %}
